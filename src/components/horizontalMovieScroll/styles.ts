import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    margin: 10,
    flex: 1,
  },
  sectionTitle: {
    marginBottom: 10,
    fontSize: 18,
    fontWeight: 'bold',
    marginHorizontal:10
  },
});
